{
  "name": "linear-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "linear": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && bun run copy:assets",
    "build:binary": "bun build src/cli/main.ts --compile --outfile dist/linear",
    "copy:assets": "mkdir -p dist/generated/manifest && cp src/generated/manifest/curated-commands.json dist/generated/manifest/curated-commands.json && cp src/generated/manifest/schema-meta.json dist/generated/manifest/schema-meta.json && (test -f src/generated/manifest/api-commands.json && cp src/generated/manifest/api-commands.json dist/generated/manifest/api-commands.json || true)",
    "generate:api-manifest": "bun run src/generated/generate-manifest.ts",
    "regenerate:schema": "bun src/generated/regenerate.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
